model = "gpt-5.4"
model_reasoning_effort = "medium"

# Reduce approval prompts while keeping commands sandboxed by default.
ask_for_approval = "on-failure"
sandbox = "workspace-write"
personality = "pragmatic"
